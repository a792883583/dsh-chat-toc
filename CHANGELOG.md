# Changelog

All notable changes to `dsh-chat-toc` will be documented in this file.

## [0.4.10] - 2026-09-12

### Docs
- **截图改用真实浏览器渲染**（Chrome headless，纯文档版本）：
  - 上一版 0.4.9 的图由 GDI+ 程序化绘制；本次改用 Chrome 加载插件**真实 CSS 与图标 SVG** 渲染，字体、间距、圆角、阴影与真实 UI 完全一致；
  - 图片内容仍为脱敏示例（通用分页问题），**不含任何真实对话或业务内容**；
  - 过滤示意图中未收藏刻度不透明度由实际的 `0.08` 提高到 `0.18`，仅为让静态图里轨道结构可辨。
- 记录一处排查经验：在 Windows 服务会话里**直接调用** `chrome`/`msedge` 并捕获 stdio 会静默无产出；改用 `Start-Process`（不接管 stdio）后即可正常渲染。
## [0.4.9] - 2026-09-12

### Docs
- **重新生成全部文档截图**（纯文档版本，无代码改动）：
  - `docs/toc-pop.png` 此前是一张**普通对话截图，根本没有拍到悬停卡片**，且包含真实聊天文本；现替换为按插件**真实 CSS 尺寸与配色**绘制的脱敏示意图（卡片标题/回复/操作栏 + 右上角四按钮工具条 + 右侧轨道）；
  - `docs/toc-bar.png` 此前仍是 0.4.3 时期的工具条位置（0.4.6 已把按钮移到模型选择器与 `···` 之间），现按当前 UI 重绘；
  - 三语 README 的图片缓存参数同步升级为 `?v=0.4.9`，确保 GitHub Camo 代理不再返回旧图。
- 说明：截图在无浏览器环境下由 GDI+ 程序化绘制（图标全部矢量绘制，未使用 emoji 字体），**不含任何真实对话或业务内容**。

## [0.4.8] - 2026-09-12

### Fixed
- **修复「官方预览卡与插件卡片同屏显示两张」的回归**：
  - 根因：0.4.7 为了不误伤右侧栏「文档预览」面板，把隐藏规则收窄成了
    `[class*="rail"] [class*="_preview"]` / `[data-slot*="rail"] ...`。
    但官方 turn rail 的 CSS Module 类名形如 `eGxaPq_frame` / `eGxaPq_preview`，
    **完全不含 `rail` 字样**，且官方包内**不存在任何 `data-slot` 属性**，
    导致该规则一个都匹配不到 —— 官方悬浮卡从未被隐藏，与插件卡片同屏。
  - 修复：改用官方卡片的**结构特征**精确定位 ——
    `[class*="_preview"]:has([class*="previewPrompt"])`。
    官方预览卡内部必定包含 `previewPrompt` 子节点，因此该选择器
    **既不受 CSS Module hash 变化影响，也绝不会误伤文档预览面板**。
  - 另加一层**不依赖 `:has()` 的 JS 兜底**（同样按 `previewPrompt` 识别），
    在不支持 `:has()` 的浏览器上也能正确隐藏官方卡片。
  - 刻意保持单条选择器而非逗号列表：旧浏览器遇到无法解析的选择器会让整条规则失效。

## [0.4.6] - 2026-09-09

### Changed
- **顶栏原生操作栏定位重构（适配 DSH 0.1.5-alpha.2+）**：
  - 彻底清理旧版向「Session 日志」更多下拉菜单内部强插挤占的逻辑，恢复官方 `···` 菜单的纯净原生布局
  - 将搜索 🔍、收藏 ⭐、代码块 ⟨/⟩、复制大纲 ⎘ 四大功能按钮精准挂载到右上角操作栏黄金位置（紧邻模型选择器与 `···` 菜单之间），视觉完美融为一体
- **大纲面板与搜索回溯增强**：
  - 支持原地平滑展开搜索框，按 `Enter` 键自动触发历史深度回溯加载
  - 完善暗色模式与明色模式的按钮自适应圆角胶囊视觉

## [0.4.5] - 2026-09-08
- 初始多维过滤胶囊与深度历史回溯支持
